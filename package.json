{
  "name": "waves",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --mode local-dev",
    "dev:published": "vite",
    "build": "tsc && vite build",
    "build:local": "tsc && vite build --mode local-dev",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "@red_j/webgl-fluid-sim": "^1.0.6"
  }
}
